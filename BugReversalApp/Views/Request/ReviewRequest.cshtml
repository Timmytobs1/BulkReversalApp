@model BugReversalApp.Models.Entities.Request
<!DOCTYPE html>
<html>
<head>
    <title></title>
</head>
<body style="font-size:1.5rem">
    <form asp-action="ApproveRequest" asp-controller="request" method="post">

        Request Id: @Model.Id<br />
        Transaction Id: @Model.TransactionId<br />
        User Id: @Model.UserId<br />
        <input type="checkbox" asp-for="Approved" name="Approved" value=true class="form-check form-check-inline bg-primary w-3" /><br />
        <i>Note that if you do not tick this box, you have automatically declined the request</i><br />
        <button class="btn btn-outline-primary" type="submit">Submit</button>
    </form>
</body>
</html>



@* @model IEnumerable<BugReversalApp.Models.Entities.Request>

<!DOCTYPE html>
<html>
<head>
    <title>Review Requests</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body class="container mt-5">
    <h1 class="text-center">Review Requests</h1>

    @if (!Model.Any())
    {
        <div class="alert alert-warning text-center">No requests available to review.</div>
    }
    else
    {
        <table class="table table-hover table-bordered mt-4">
            <thead class="thead-dark">
                <tr>
                    <th>Transaction ID</th>
                    <th>User First Name</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var request in Model)
                {
                    <tr>
                        <td>@request.TransactionId</td>
                        <td>@request.User?.FirstName ?? "Not Available"</td> <!-- Safeguard if User is null -->
                        <td>
                            <a asp-action="ReviewRequest" asp-controller="Request" asp-route-id="@request.Id" class="btn btn-outline-primary btn-sm">
                                Review
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</body>
</html>
 *@